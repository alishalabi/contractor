<h1>Students</h1>

<a href="/students/new">New Student</a>

<div class="row">
  {{# each students}}
    <div class="col-sm-3">
    <h2><a href="/students/{{this._id}}">{{this.name}}</a></h2>
      <small>{{this.bio}}</small><br>
      <small>{{this.goal}}</small><br>
    </div>
  {{/each}}
</div>
